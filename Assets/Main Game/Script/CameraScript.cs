using UnityEngine;

public class CameraScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    private void Update()
    {
        //Camera.main.orthographicSize = (3.970462f / Screen.width * Screen.height / 1.5f);
    }
}